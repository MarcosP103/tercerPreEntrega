<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Completar Perfil</title>
</head>
<body>
    <h1>Completar Perfil</h1>
    <form action="/api/sessions/profile/edit" method="POST" id="profileForm">
        <div>
            <label for="first_name">Nombre:</label>
            <input type="text" class="form-control" id="first_name" name="first_name" value="{{getProperty user 'first_name'}}">
        </div>
        <br>
        <div>
            <label for="last_name">Apellido:</label>
            <input type="text" class="form-control" id="last_name" name="last_name" value="{{getProperty user 'last_name'}}">
        </div>
        <br>
        <div>
            <label for="email">Email:</label>
            <input type="email" class="form-control" id="email" name="email" value="{{getProperty user 'email'}}">
        </div>
        <br>
        <div>
            <label for="age">Edad:</label>
            <input type="number" class="form-control" id="age" name="age" value="{{getProperty user 'age'}}">
        </div>
        <br>
        <div>
            <label for="password">Contraseña:</label>
            <input type="password" class="form-control" id="password" name="password" value="{{getProperty user 'password'}}">
        </div>
        <br>
        <div>
            <label for="confirm_password">Confirmar Contraseña:</label>
            <input type="password" class="form-control" id="confirm_password" name="confirm_password" value="{{getProperty user 'password'}}">
        </div>
        <br>
        <button type="submit" class="btn btn-success" >Guardar Cambios</button>
    </form>
    <br>
    <form action="/profile" method="GET">
        <button type="submit" class="btn btn-warning" >Cancelar</button>
    </form>
</body>
</html>
