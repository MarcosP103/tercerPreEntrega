const fs = require("fs").promises

class CartManager{
    constructor(filePath){
        this.path = filePath
    }

    async createCart() {
        try {
            

        } catch (error) {

        }
    }

}